{
  "name": "proyek-fullstack",
  "version": "1.0.0",
  "description": "Proyek Fullstack",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" --kill-others-on-fail --prefix \"[{name}]\" --names \"BACKEND,FRONTEND\" --handle-input false"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}